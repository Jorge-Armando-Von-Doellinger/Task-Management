logging:
  level:
    root: ERROR
    org:
      hibernate: ERROR
    org.springframework: ERROR

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boards?createDatabaseIfNotExist=true
    username: root
    password: NewPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
